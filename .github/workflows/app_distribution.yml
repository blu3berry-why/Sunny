name: Build and Release
on:
  push:
    branches: [ master ]
    tags: [ v* ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17 (required by AGP 8.11)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle cache
        uses: gradle/actions/setup-gradle@v3

      - name: Make wrapper executable
        run: chmod +x gradlew

      - name: Build debug APK (composeApp)
        run: ./gradlew :composeApp:assembleDebug --stacktrace

      - name: Verify APK exists
        run: |
          ls -la composeApp/build/outputs/apk/debug || true
          test -f composeApp/build/outputs/apk/debug/composeApp-debug.apk

      - name: Upload to Firebase App Distribution
        uses: wzieba/Firebase-Distribution-Github-Action@v1.7.1
        with:
          appId: ${{ secrets.SUNNY_ANDROID_APP_ID_DEBUG }}
          serviceCredentialsFileContent: ${{ secrets.SERVICE_ACCOUNT_CREDENTIALS_FILE_CONTENT }}
          groups: family-and-friends
          file: composeApp/build/outputs/apk/debug/composeApp-debug.apk
